import Layout from "@/components/layout";
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Main',
  description: 'Holiday Calendar Main'
}

export default function Main() {
  return (
    <Layout>
      <a></a>
    </Layout>
  );
}
